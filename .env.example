# Copy to .env and fill in.
# Get Spotify credentials from https://developer.spotify.com/dashboard
# Redirect URI: add this exact URL in your Spotify app (Better Auth callback; use 127.0.0.1 for dev)
# Dev: http://127.0.0.1:5173/api/auth/callback/spotify
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SESSION_SECRET=

# Optional: only this Spotify user ID can use the app (leave empty to allow any logged-in user)
ALLOWED_SPOTIFY_ID=

# Full app URL for Better Auth (callbacks/redirects). Dev: http://127.0.0.1:5173  Prod: https://your-domain.com
BETTER_AUTH_BASE_URL=

# Neon Postgres: create a project at https://neon.tech and copy the connection string
DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require

# AI playlist generation (https://console.anthropic.com/)
ANTHROPIC_API_KEY=
