# Copy to .env and fill in.
# Get Spotify credentials from https://developer.spotify.com/dashboard
# Redirect URI: add this exact URL in your Spotify app (Spotify does not allow localhost; use 127.0.0.1)
# Use 5173 for `pnpm dev`, or 3847 for the desktop app (`pnpm run desktop`)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/auth/spotify/callback
SESSION_SECRET=

# Neon Postgres: create a project at https://neon.tech and copy the connection string
DATABASE_URL=postgresql://user:password@host.neon.tech/neondb?sslmode=require

# AI playlist generation (https://console.anthropic.com/)
ANTHROPIC_API_KEY=
